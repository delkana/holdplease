node_modules/
.claude/
.DS_Store
*.log

# your sound files stay local unless you choose to commit them
# (.railwayignore keeps them in the deploy even though git skips them)
audio/**/*.mp3
audio/**/*.ogg
audio/**/*.wav
